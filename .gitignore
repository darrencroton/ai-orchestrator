.DS_Store
__pycache__/

.claude/

# Orchestration artefacts (runs/ and current symlink are transient; index.json may be committed deliberately)
.ai-orchestrator/runs/
.ai-orchestrator/current
.ai-orchestrator/.index.lock

# Session handoff document (ephemeral per session; commit deliberately if needed)
HANDOFF.md
